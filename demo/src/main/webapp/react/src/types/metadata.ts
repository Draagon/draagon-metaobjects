/**
 * Type definitions for the demo (simplified version)
 */

export enum ViewMode {
  READ = 'READ',
  EDIT = 'EDIT', 
  HIDE = 'HIDE'
}

export enum FieldType {
  STRING = 'string',
  INT = 'int',
  LONG = 'long',
  DOUBLE = 'double',
  BOOLEAN = 'boolean',
  DATE = 'date',
  OBJECT = 'object',
  OBJECT_ARRAY = 'objectArray'
}